# This folder holds the trained model files (generated after training).
# The actual .pth and .pkl files are excluded from git via .gitignore
# because they are large binary files.
#
# After cloning, run:
#   python model/train.py --mode cnn --dataset-path data/sample_fingerprints
# to generate these files.
